openapi: 3.0.3
info:
  title: 냠냠투게더 API — A·C 파트 (추천 서버)
  version: 0.2.0
  description: |
    이 명세는 **우리 소유 엔드포인트(추천 조회·정렬, 식당 등록)** 만 다룬다.
    인증/세션/참여/투표 엔드포인트는 공통/B파트 소유 → 여기서는 제외.
    상세는 `docs/api-spec.md`. 인증은 토스 로그인 → 서버 발급 JWT(Bearer).
servers:
  - url: http://localhost:3000
    description: 로컬 개발
tags:
  - name: 추천
  - name: 등록

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    Error:
      type: object
      properties:
        code: { type: string, example: BAD_REQUEST }
        message: { type: string }
      required: [code, message]
    Recommendation:
      type: object
      description: 후보 스냅샷 + 표시 시점 라이브 합성 필드(name/rating 등).
      properties:
        id: { type: string, format: uuid }
        session_id: { type: string, format: uuid }
        place_id: { type: string, format: uuid, description: places.id 참조 }
        source: { type: string, enum: [google, owner, community] }
        place_type: { type: string, enum: [drink_required, compatible, general] }
        rank: { type: integer }
        relaxed: { type: boolean, description: 조건 완화 포함 여부 }
        voteCount: { type: integer, description: stage2 득표 수(B 집계) }
        # 아래는 표시 시점 라이브 조회 합성값 (DB 저장 안 함)
        name: { type: string, nullable: true, description: 라이브(google) 또는 등록값 }
        rating: { type: number, nullable: true, description: 라이브 google rating }
        userRatingCount: { type: integer, nullable: true, description: 라이브 리뷰 수 }
        priceLevel: { type: integer, nullable: true }
        distance_m: { type: integer, nullable: true }
    Place:
      type: object
      properties:
        placeId: { type: string, format: uuid }
        source: { type: string, enum: [owner, community] }
        stationId: { type: string }
        name: { type: string }
        lat: { type: number }
        lng: { type: number }
        category: { type: string, nullable: true }
        priceLevel: { type: integer, nullable: true }
        openDate: { type: string, format: date, nullable: true }

security:
  - bearerAuth: []

paths:
  /sessions/{id}/recommendations:
    get:
      tags: [추천]
      summary: 추천 후보 조회 (+ 정렬·득표수)
      description: |
        status가 voting 이상일 때 유효. 아직이면 202.
        표시용 name/rating 등은 최종 후보만 라이브 조회 후 서버가 합쳐 반환.
        구글 데이터 포함 시 "Powered by Google" 출처 표기 동반.
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - name: sort
          in: query
          required: false
          schema: { type: string, enum: [review_count, rating, random] }
          description: 미지정 시 세션 sort_mode 사용.
      responses:
        '200':
          description: 후보 목록
          content:
            application/json:
              schema:
                type: object
                properties:
                  recommendations:
                    type: array
                    items: { $ref: '#/components/schemas/Recommendation' }
                  relaxed: { type: boolean }
                  attribution: { type: string, example: Powered by Google }
        '202':
          description: 아직 집계 전
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
              example: { code: NOT_READY, message: 아직 집계 전입니다 }
        '404':
          description: 세션 없음
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /sessions/{id}/sort:
    patch:
      tags: [추천]
      summary: 후보 정렬 모드 변경 (세션 공유)
      description: 개인별 아님. voting 단계에서 허용. 후보·집계는 불변(표시 순서만).
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sortMode]
              properties:
                sortMode: { type: string, enum: [review_count, rating, random] }
      responses:
        '200':
          description: 변경됨
          content:
            application/json:
              schema:
                type: object
                properties: { sortMode: { type: string } }
        '400':
          description: 잘못된 sortMode
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /places:
    post:
      tags: [등록]
      summary: 점주/시민 식당 등록 (MVP 스텁)
      description: first-party 저장. 검증·심사 로직은 추후.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [source, stationId, name, lat, lng]
              properties:
                source: { type: string, enum: [owner, community] }
                stationId: { type: string }
                name: { type: string }
                lat: { type: number }
                lng: { type: number }
                category: { type: string, nullable: true }
                priceLevel: { type: integer, nullable: true }
                openDate: { type: string, format: date, nullable: true }
      responses:
        '201':
          description: 등록됨
          content:
            application/json:
              schema:
                type: object
                properties: { placeId: { type: string, format: uuid } }
        '400':
          description: 필수값 누락
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    get:
      tags: [등록]
      summary: 역의 등록 식당 목록
      parameters:
        - { name: stationId, in: query, required: true, schema: { type: string } }
      responses:
        '200':
          description: 목록
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Place' }
